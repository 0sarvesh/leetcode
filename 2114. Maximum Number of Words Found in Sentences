class Solution {
    public int mostWordsFound(String[] sentences) {
        int max = 0;
        for(String sent:sentences){
            // splits accordingly no of sentences
           int length =sent.split(" ").length;
           //finds the max
            max = Math.max(max,length); 
        }
    return max;
    }


}
